name: ait-foodapp-group-c03

services:
  # ============================================================================
  # PostgreSQL Database Service
  # ============================================================================
  postgres-db:
    image: postgres:16-alpine
    container_name: fooddelivery-postgres-03
    restart: unless-stopped
    environment:
      POSTGRES_DB: ${POSTGRES_DB:-fooddelivery03}
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    ports:
      - "${POSTGRES_PORT:-5003}:5432"
    volumes:
      - postgres_data03:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-postgres} -d ${POSTGRES_DB:-fooddelivery03}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    networks:
      - fooddelivery-network03

  # ============================================================================
  # Spring Boot Backend API Service
  # ============================================================================
  backend:
    build:
      context: ./food-delivery-backend
      dockerfile: Dockerfile
    container_name: fooddelivery-backend-03
    restart: unless-stopped
    ports:
      - "${BACKEND_PORT:-8083}:8080"
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres-db:5432/${POSTGRES_DB:-fooddelivery03}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER:-postgres}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
      SPRING_JPA_HIBERNATE_DDL_AUTO: update
      SERVER_PORT: 8080
    depends_on:
      postgres-db:
        condition: service_healthy
    networks:
      - fooddelivery-network03

  # ============================================================================
  # React / Vite Frontend Service
  # ============================================================================
  frontend:
    build:
      context: ./food-delivery-frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8083/api/users}
    container_name: fooddelivery-frontend-03
    restart: unless-stopped
    ports:
      - "${FRONTEND_PORT:-4003}:80"
    depends_on:
      - backend
    networks:
      - fooddelivery-network03

# ==============================================================================
# Persistent Volumes
# ==============================================================================
volumes:
  postgres_data03:
    name: fooddelivery_postgres_data03

# ==============================================================================
# Networks
# ==============================================================================
networks:
  fooddelivery-network03:
    name: fooddelivery-network03
    driver: bridge
